package com.example.assignment1_ofek_fanian.utilities

object Constants {
    object Game {
        const val ROWS = 7  // Must match the rows in XML!
        const val COLS = 3
        const val LIVES = 3
        const val TIMER_DELAY = 500L // Game speed (milliseconds)
    }

    object Data {
        const val KEY_STATUS = "KEY_STATUS" // Key for passing data via Intent
    }
}